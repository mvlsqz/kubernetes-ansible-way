- name: IP address for the LB
  gcp_compute_address:
    auth_kind: "{{ auth_kind }}"
    name: kubernetes-hardway-mvlsqz
    project: "{{ project }}"
    region: "{{ region }}"
    scopes: "{{ scopes }}"
    service_account_file: "{{ service_account_file }}"
    state: "{{ state }}"
